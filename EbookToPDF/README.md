# MobiToPDF GUI

一个简单的 Python GUI 工具：将 `.mobi` `.epub` 电子书转换为 `.pdf`，支持：

- 可选择字体（内置字体 + 自定义 TTF）
- 可设置 PDF 上下左右边距
- 可选输入书名、作者
- 若输入书名，会自动生成“白底黑字”封面页
- 转换完成后可选择 PDF 保存位置

## 安装依赖

```bash
pip install -r requirements.txt
